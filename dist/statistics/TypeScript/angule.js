"use strict";
function deg2rad(deg) {
    //角度转弧度
    // body...
    return deg * (Math.PI / 180);
}
function rad2deg(rad) {
    //弧度转角度
    return rad * (180 / Math.PI);
}
//# sourceMappingURL=angule.js.map