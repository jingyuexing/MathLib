package com.mathlib;

import java.util.ArrayList;

class Softmax{
  public static double[] Softmax(double... args){
    ArrayList<Double> thisValues;
    ArrayList<Double> softmax;
    for(int i=0;i<args.length;i++){
      
    }
  }
  private static double exp(double x){
    return Math.pow(Math.E,x);
  }
}